diff --git a/Cargo.toml b/Cargo.toml
index 19d4caa..3d99bdf 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -5,8 +5,7 @@ members = [
     "components/xpath",
     "ffi/capi",
     "ports/servoshell",
-    "tests/capi",
-    "tests/unit/*",
+    "support/content-packer",
 ]
 default-members = ["ports/servoshell"]
 exclude = [".cargo", "support/crown"]
@@ -99,7 +98,6 @@ futures-core = { version = "0.3", default-features = false }
 futures-intrusive = "0.5"
 futures-util = { version = "0.3", default-features = false }
 gaol = "0.2.1"
-gilrs = "0.11.1"
 gleam = "0.15"
 glib = "0.22"
 glib-sys = "0.22"
@@ -141,7 +139,7 @@ inventory = "0.3.24"
 ipc-channel = "0.22"
 itertools = "0.15"
 jni = "0.22"
-js = { package = "mozjs", version = "=0.21", default-features = false, features = ["intl", "libz-sys"] }
+js = { package = "mozjs", version = "=0.21.6", default-features = false, features = ["intl", "libz-sys"] }
 k12 = "0.5"
 keyboard-types = { version = "0.8.3", features = ["serde", "webdriver"] }
 kurbo = { version = "0.13.1", features = ["euclid"] }
@@ -155,7 +153,7 @@ mime = "0.3.13"
 mime_guess = "2.0.5"
 ml-dsa = { version = "0.1", features = ["zeroize"] }
 ml-kem = { version = "0.3", features = ["getrandom", "pkcs8", "zeroize"] }
-mozangle = "0.6"
+mozangle = "0.7"
 napi-derive-ohos = "1.2.0"
 napi-ohos = "1.2.0"
 nix = "0.30"
@@ -216,6 +214,13 @@ rustc-hash = "2.1.2"
 rustls = { version = "0.23", default-features = false, features = ["logging", "std", "tls12"] }
 rustls-pki-types = "1.14"
 rustls-platform-verifier = "0.7.0"
+# `build-from-source`: builds SDL3 from vendored source as part of the Rust build (via `cmake`
+# + a C/C++ toolchain, both already required for other native deps here) instead of the crate's
+# own default of requiring a pre-installed system SDL3 via pkg-config/vcpkg -- keeps this
+# self-contained/versioned by Roves, the same reasoning `docs/DEPENDENCY_REVIEW.md` already
+# applies to the rest of the embedded engine, and avoids a second native-installer saga on top
+# of GStreamer's (see CUSTOMIZATIONS.md).
+sdl3 = { version = "0.20", default-features = false, features = ["build-from-source"] }
 sea-query = { version = "1.0.1", default-features = false, features = ["backend-sqlite", "derive"] }
 sea-query-rusqlite = "0.8.0"
 selectors = { version = "0.40" }
@@ -246,11 +251,12 @@ swapper = "0.1"
 syn = { version = "2", default-features = false, features = ["clone-impls", "derive", "parsing"] }
 synstructure = "0.13"
 sys-locale = "0.3"
+sysinfo = { version = "0.38" }
 taffy = { version = "0.12.1", default-features = false, features = ["calc", "detailed_layout_info", "grid", "std"] }
 tempfile = "3"
 tendril = { version = "0.5", features = ["encoding_rs"] }
-tikv-jemalloc-sys = "0.6.1"
-tikv-jemallocator = "0.6.1"
+tikv-jemalloc-sys = "0.7.1"
+tikv-jemallocator = "0.7.0"
 time = { package = "time", version = "0.3", features = ["large-dates", "local-offset", "serde"] }
 tokio = "1"
 tokio-rustls = { version = "0.26", default-features = false, features = ["logging"] }
